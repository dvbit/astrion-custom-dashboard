# Changelog

All notable changes to this project are documented here.
Format based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [1.2.0] - 2026-09-19

### Added

- **Web builder: forms for the last four raw-JSON card types** (spec: [`docs/EDITOR_FORMS_SPEC.md`](docs/EDITOR_FORMS_SPEC.md)), in a new `js/cards-advanced.js` hooked into the existing `cards.js` dispatch:
  - `speaker_group` — master picker (device catalog), display name, reorderable speaker list.
  - `monitor` — title and a reorderable list of any-domain entities with optional labels.
  - `row` — list of child cards, each edited in the same dialog with its own type's form (a `row` inside a `row` works too). Children stay in a draft until the row itself is saved; Cancel/✕ inside a child returns to the row.
  - `picture_elements` — image path, aspect ratio, entity/service elements, radar overlay (every field `PictureElementsCard.kt` reads) and vacuum overlay (room positions, dock, room-clean buttons/service). A local copy of the floor plan (browser file picker, never uploaded or saved) is the background of a canvas where elements, radar origin, room positions and dock are placed by dragging (mouse or touch).
  - `title` — title and subtitle tap actions (`title_*` / `subtitle_*`: scene/script, Harmony activity/command, local IR, composed Activity, plus optional page).
- Phone preview renderers for `speaker_group`, `monitor`, `row` and `picture_elements` (live HA values when the builder is served by the device).

### Changed

- Card type picker: the four types above moved from "Advanced (raw options)" to a new "Media & layout" group.

### Fixed

- **Re-saving a card through its form silently dropped every option the form didn't model** — e.g. `pin: "bottom"`, a `scene_grid`'s `layout: "row"`, a `climate`'s `step`, a `tv_remote`'s `commands`/`apps`. Unmodeled keys are now carried over when the card keeps its type (`FORM_MANAGED_KEYS` / `preserveUnknownOptions()`); keys a form does manage are still removed when cleared.

## [1.1.4-beta] - 2026-09-19

### Added

- Italian localization support. Thanks to [@dvbit](https://github.com/dvbit) for the contribution! 🙏
- **Automated page routing via Home Assistant entities** (`PageConfig.openWhenEntity`) — a page can now declare an explicit entity ID string to monitor. When this entity changes state or triggers, the dashboard can automatically bring this page to the foreground (e.g., flipping automatically to a "Weather" or "Meteo" page when rain is detected, or showing a camera stream page when a doorbell rings). `DashboardLoader.kt` properly parses and round-trips the optional `"openWhenEntity"` field from `dashboard.json`, mapping it cleanly at runtime to `openWhenEntityPages` without risking crashes or regressions on older configurations.
- **Linked pages** (`PageConfig.linkedPage`) — a page can now name another page to jump straight to on a swipe UP from its own [`PageIndicator`], the vertical counterpart of the existing horizontal `parent`/`parentKey` relationship. Meant for a per-card "more options" page (e.g. extra Apple TV controls) sitting just below whatever card it belongs to: give the linked page its own `parent` pointing back, and BACK/its chevron/the tap-to-return flow already in place works unchanged. `DashboardLoader.kt` round-trips an optional `"linkedPage"` field; existing `dashboard.json` files with none keep working unchanged. Web builder (`docs/index.html`/`js/pages.js`): new "Linked page" picker on the page dialog, renamed/deleted pages keep every `linkedPage` reference in sync the same way `parent` already did.
- **Conditionally-hidden pages** (`PageConfig.hiddenUnlessActivity`) — a page's dot can now be left out of `PageIndicator` until a specific Activity is actually running (same id space as a scene_grid tile's own `"activity"` field, picked from the same dropdown), reappearing automatically the moment it is and disappearing once it stops — decluttering the indicator for a page tied to a device that isn't always in use. Deliberately dots-only: the pager itself (`pageCount`/`pagerState`/`navTarget`/`navigateToPage`) always covers every page in `AppConfig.pages`, unfiltered, regardless of this field — an earlier version of this feature gated the pager itself on live Activity state, which raced a scene_grid tile's own "activity"+"page" combo tap and silently broke navigating to a page right as its Activity started (`ActivityDispatcher.switchActivity` only calls `markActiveById` — what this field actually reads — *after* every device command in the plan has gone out, well after the tile's own synchronous tap handler had already given up looking for a still-hidden page). `Dashboard.kt` computes a lightweight `activeActivityIds` set (`ActivityRuntime.activeByRoom`'s non-null values) purely for `PageIndicator` to filter dots with; nothing else touches it. `DashboardLoader.kt` round-trips an optional `"hiddenUnlessActivity"` field. Web builder: new "Hide unless this Activity is running" picker on the page dialog, sourced from the Activities tab (same list/format as a scene_grid tile's own activity picker in `cards.js`).
- **A scene_grid tile starting an Activity now navigates through that Activity's own `page`, not a redundant field on the tile.** `ActivityDispatcher.switchActivity` calls the new `navigateToPage` it's constructed with right after `markActiveById` — i.e. once the Activity is actually confirmed active, not speculatively before any of its device commands have gone out. `SceneGridCard`'s own `onTap` no longer fires a tile's `"page"` field when that same tile also sets `"activity"`: this is what `ActivityConfig.page` was already designed for (the web builder's `cards.js` already auto-syncs a scene_grid item's `activity` field into `activity.page` whenever a card sets one — see `addGridItem`'s existing comment on that), and letting `ActivityDispatcher` fire it at the right moment is what actually fixes the redundant-and-racy pattern of a tile navigating on its own, rather than the field rename above merely making the symptom (a silently un-hidden page) go away. A tile's own `"page"` still works exactly as before on any tile that doesn't also set `"activity"`.
- **"Open Settings" needed no new code at all — it already existed as a regular hotkey action.** `openOverlay: "settings"`/`"activities"` (`hotkeys`/`longHotkeys`, global or per-page, any key, either press length) was already fully wired end-to-end (`ConfigServer`, `MainActivity`'s hotkey engine, the Hotkeys tab's own `openOverlay` UI in `hotkeys.js`) — the only thing actually missing was that the real device POWER button can't be bound to anything at all (Android's window manager intercepts every real `KEYCODE_POWER` press — short, long, even double — before any third-party app, even a Device Owner one, ever sees it; not fixable from an app, full stop), and this app's own on-screen POWER tile is already spoken for (`HardwareKey.POWER`, raw code 132 — a genuinely deliverable KeyEvent, just tied to a software control, not the physical button) — it's what a dashboard uses to turn its own AV devices on and off, so it must stay untouched. No restart feature was added either: the device's real side button already does that natively (Android's own short-press-sleep/long-press-restart-or-power-off), which is exactly the "official, like every Android device" behavior wanted in the first place — nothing to build there. `hotkeys.js`'s `openOverlay` picker had two stale labels left over from *before* Activities and Settings swapped gesture spots (see the very next entry below) — those are fixed. Pick any key that isn't POWER from the Hotkeys tab, `openOverlay: "settings"`.
- **Astrion IR Extenders** (network-connected WiFi IR blasters, see the astrion-ir-extender project) as a full alternative to the device's own built-in IR blaster, for commands that need to reach a closed cabinet or another room. No Home Assistant required.
  - `IrDeviceConfig` gains a `target` field (`IrTarget.Local` — the existing `ConsumerIrManager` behavior, still the default — or `IrTarget.Extender(extenderId)`). Lives on the IR device itself, not on individual cards/hotkeys, since a device is physically in one place: point a device at an extender once, and every card/hotkey/Activity that already references it automatically follows. `DashboardLoader.kt` round-trips an optional `"target"` field; every existing `dashboard.json` with none keeps working unchanged.
  - New `ExtenderConfig` (`RemoteSettings.extenders()`/`saveExtenders()`, mirroring Harmony hub storage), `ExtenderClient`/`ExtenderRegistry` (`com.custom.astrion.extender`) — plain-HTTP client posting to an extender's `POST /pronto`.
  - **Batched, sequenced sends**: `ExtenderClient.sendBatch()` posts several Pronto codes — each with its own delay-before-transmit — in a single request, instead of one HTTP round-trip per command. `ActivityDispatcher` (extracted from `Dashboard.kt`; see Changed below) builds an ordered `StepPlan` for an Activity switch/stop and merges consecutive same-extender steps into one batch, while local IR/Harmony/HA steps still dispatch individually with a real delay — this is what actually fixes commands silently going missing when two arrive close together on the same extender (e.g. switching Activities that share one), which a naive one-request-per-command design couldn't guarantee.
  - `IrStepConfig` gains an optional `pronto: String?` — the original Pronto hex string a resolved step was decoded from, when known, forwarded to an extender as-is instead of re-deriving it from the already-decoded `freq`/`pattern`. Populated for ir-database (`SdCardRef`) devices; **not yet populated for `Inline`-sourced devices** (dashboard.json only persists their decoded `freq`/`pattern` today) — those log a warning and no-op rather than send anything if pointed at an extender.
  - Web builder: new "IR Extenders" section (`devices.html`/`devices-page.js`) — add/edit/remove extenders by name + IP/hostname + MAC address, same all-or-nothing save as the Harmony hubs section. `localId` is derived from the MAC (`ext_<mac>`, normalized regardless of how it's typed — colons, dashes, or none), guaranteeing the same physical unit keeps the same id across a rename, IP change, or remove-and-re-add — the MAC field is required specifically to make this possible; an earlier attempt at deriving the id from a MAC-suffixed mDNS hostname string never actually worked (ESPHome's `name_add_mac_suffix` appends only 6 hex digits, not the 12 the matching regex expected). New "where should this device's commands actually get sent from?" selector on the IR device editor, writing `target` in the format above; blocked for hand-pasted ("inline") commands with an explanation, since those can't target an extender yet (see `IrStepConfig` above).
  - `ConfigServer.kt`: `parseExtenderRows()` wired into `handleSaveConnection()`; `GET /devices-config` now includes `extenders`.

### Changed

- **"Active Activities" overlay moved from the bottom page-indicator's swipe-up onto the top status bar's swipe-down** — the spot Settings used to occupy, now that Settings is just a regular assignable hotkey instead of a fixed gesture. `TopStatusBar`'s callback renamed `onSwipeDownToSettings` → `onSwipeDown` to match (it's the caller's choice now, not baked into the name); `PageIndicator`'s `onSwipeUpToActivities` renamed `onSwipeUpToLinkedPage` since swipe-up now drives the new linked-page feature above. `ActivitiesOverlay`'s own dismiss gesture strip moved from top (swipe down) to bottom (swipe up) to match — closing it now pulls it back out the way it came in, the same convention `SettingsOverlay` already used.
- `DashboardLoader.kt` now actually parses a page's `"parentKey"` field from `dashboard.json` — it was documented as configurable but silently ignored, every page defaulting to `"BACK"` regardless of what was saved. Found while adding the two new page fields above; unrelated to them otherwise.
- `HardwareKeyRouter` gains `isLongBound()`, mirroring the existing `isShortBound()` — general-purpose, for any future fallback binding that needs to check long-press the same way the parent-key fallback already checks short-press.
- `Dashboard()`'s IR/Activity dispatch logic (`sendIrCommand`, `dispatchActivityCommand`, `dispatchActivityPower`, `switchActivity`, `startActivity`, `stopActivity`) extracted into a new `ActivityDispatcher` class (`com.custom.astrion.ui`) — behavior-preserving except for the batching described above, which only this extraction made practical to add cleanly.

### Fixed

- **Editing an existing IR device from the "Devices" page silently duplicated it instead of updating it.** `editIrDevice()` set `editingIrDevice` *before* calling `openDeviceForm('ir')`, which calls `cancelIrDeviceEdit()` internally and resets it straight back to `null` — so `saveIrDevice()` always thought it was creating a new device. Found while wiring the extender target selector into the same form. Fixed by setting `editingIrDevice` after `openDeviceForm()` returns instead of before.
- **The builder's "Install on this device" button always installed the beta, even when the page was advertising an official update, leaving the official version stuck on the old one.** `loadStableBadge()` only ever rendered a manual download link for the official release; `toggleBetaBadge()` was the only badge wired to a real one-tap install button (`installBetaUpdate()` → `POST /install-beta-update`), so it was the only working button on the page regardless of which version the user actually meant to install — the official `POST /install-update` route (`ConfigServer.handleInstallUpdate()`) has existed since it was added but was never called from any client code. `loadStableBadge()` now gets the same button, wired to a new `installStableUpdate()` (`POST /install-update`); official and beta buttons now say "Install this official update" / "Install this beta update" (`installOfficial`/`installBeta`) instead of sharing one ambiguous label.
- **Harmony IR commands sent to the hub didn't match the button actually pressed, so some (or most) buttons silently did nothing.** `HarmonyHubClient.parseConfig()` used a Harmony device's `function.name`/`.label` fields as the literal IR command sent via `sendCommand()` — but those are just the hub's own display labels and can differ from the real command it expects (e.g. a button labeled "Select" whose actual IR command is `"OK"`, nested inside the function's `action` field as `{"command":"OK",...}`). Sending the display label instead of the real command meant the hub simply ignored it — no error, no feedback, just a dead button. `parseConfig()` now extracts the command from `action.command` when present, falling back to `name` only if `action` is missing or malformed; `label` still drives what's shown in the UI. Split `parseConfig()` into smaller `parseDevices()`/`parseDevice()`/`parseFunctions()`/`parseCommand()`/`extractIrCommand()`/`parseActivities()` helpers along the way — the added branching pushed cyclomatic complexity past detekt's threshold in one function.
- **A Harmony hub's `localId` changed every time it was removed and re-added, even for the exact same physical hub, orphaning any hotkey or scene tile referencing it.** `saveHarmonyHub()` (web configurator, `/`) generated a random `localId` (`'hub_' + Date.now()... + Math.random()...`) for every new hub row, with no link to the hub's own identity. Deleting a hub and adding it back — same IP, same Logitech `hubId` — produced a brand-new `localId`, silently breaking every `dashboard.json` reference (`"hub": "hub_..."`) built against the old one. `localId` is now derived deterministically from the hub's own `hubId` when known (`'hub_' + hubId`), so re-adding the same physical hub restores the same `localId` and existing references keep working; only falls back to a random id when `hubId` isn't set yet (e.g. before running "Detect"). Same fallback logic mirrored server-side in `ConfigServer.kt`'s `parseHubRows()` for consistency. Not retroactive — hubs already carrying a random `localId` from before this fix aren't remapped.

## [1.1.0] - 2026-09-06

### Added

- **Devices catalog (`/`), rebuilt as a proper "add a device" hub instead of a connection form.** Opening the remote's own IP now lands on a hero + four category tiles (Home Assistant, Harmony, IR Devices, Apple TV — the last a placeholder for direct control, not built yet, present so cards will be ready to reference it once it is), each with a live device count. Tapping a tile drills into that category's own list + **"+ Add device"** flow, Home-Assistant-integrations style, instead of the old single long scrolling form. `ConfigServer.kt`'s previous `serveForm()` (one large server-rendered HTML string) is replaced by two static single-page apps served from `assets/docs/`: `devices.html`/`js/devices-page.js` (this new page, root `/`) and the existing `index.html`/`js/*.js` (the dashboard/card builder, `/builder/`) — both now share the same static-asset serving path (`serveDocsAsset()`), so any file dropped into `assets/docs/` (styles, scripts, images) is reachable from either page without a route added per file.
- **Home Assistant device catalog.** Cards that reference an HA entity (Light, Switch, Cover, Climate, Media Player, Camera, Fan, Vacuum, Weather, Select) no longer take a free-typed `entity_id` — pick a domain and a live entity on the Devices page, give it a name (pre-filled from HA's own `friendly_name`, editable), and it shows up by that name in the matching card's picker in `/builder/`. New `haDevices` array in `dashboard.json` (parallel to `irDevices`), new `GET /devices-config` endpoint (full HA connection + Harmony hub read-back, used to pre-fill the edit forms — `GET /harmony-hubs` stays id+name-only, unchanged, still used by the card/hotkey pickers). Editing an existing card that references an entity *not* in the catalog (an older dashboard, or one hand-edited via raw JSON) doesn't silently blank the field — an extra "*(not in catalog)*" option is injected so the real value round-trips even though it can't be changed without adding it to the catalog first.
- **`Cache-Control: no-cache, no-store, must-revalidate`** on every `docs/` asset response (`serveDocsAsset()`) — these ship inside the APK and change on every app update; a stale cached `.js` silently paired with a fresh `.html` (or vice versa) produces exactly the kind of confusing, hard-to-reproduce bug this is meant to rule out.
- **Charging screen.** While the remote is genuinely sitting on its charging dock, the dashboard is replaced with a big battery percentage and a pulsing charging glyph — same idea as a phone's own charging screen. Auto-dims (window brightness down to near-zero) after 15s with no touch, undims instantly on the next one; fully restored the moment it's picked up or unplugged.
  - **Docked vs. plugged into a computer for development is a real distinction here, and a non-obvious one.** Both report identically through `BatteryManager` — confirmed on real hardware, this device's dock has two power-only charging pins, no USB data lines at all, and Android still classifies that as generic "AC" charging just like a PC's USB port would. What actually differs is whether a USB *data* connection exists: `UsbManager`'s own connection state (`mConnected`/`mConfigured` in `dumpsys usb`) reads `false` on the dock and `true` over a real cable. That state isn't exposed through the public `UsbManager` SDK constants (`ACTION_USB_STATE`/`USB_CONNECTED` exist in AOSP and fire at runtime, but aren't in the public stub jar) — used as raw string literals instead, which is legitimate since both are just string values handed to fully public APIs (`IntentFilter.addAction`, `Intent.getBooleanExtra`), not a reach into a hidden class member.
  - New `ChargeDockMonitor.kt`: owns the detection (a `BroadcastReceiver` on `ACTION_POWER_CONNECTED`/`_DISCONNECTED` + the USB state action) and the dim timer, as its own class rather than inline in `MainActivity.kt` — which was starting to accumulate too many unrelated concerns in one place (`detekt`'s `LargeClass` agreed). `MainActivity.kt` now just owns one instance and delegates: `start()`/`stop()` from its own lifecycle methods, `onUserInteraction()` (the standard Android hook that fires on any touch/key regardless of which view or Compose node consumed it — no separate touch listener needed) forwarded straight through.
  - New `ChargingScreen.kt`, themed via the same `ProvideTheme`/`LocalTheme` system every other screen uses. `rememberBatteryState()` and `batteryGlyph()` — both previously private to `Topstatusbar.kt` — are now shared between the status bar's compact battery indicator and this screen's much larger one, rather than duplicated.
  - 2 new strings (`charging_screen_charging`/`charging_screen_full`) in `values/`/`values-fr/`.
- **IR devices can now reference the external [ir-database picker](https://dckiller51.github.io/astrion-ir-sniffer/) instead of embedding pre-resolved codes.** Previously every `dashboard.json` had to carry a fully-resolved `{freq, pattern}` table for every IR command, baked in once by the web builder at build time from either a hand-pasted Pronto code or a bundled `ir-database/` catalog. That catalog (`docs/ir-database/` and its `src/main/assets/docs/ir-database/` mirror) is **removed** in this release — instead, pick the devices you actually own on the picker, copy the generated JSON files onto the remote's `/sdcard/astrion/ir-database/`, and reference them from the web builder ("2. IR Devices" → "Reference the ir-database", category + brand + model). The app resolves Pronto → `{freq, pattern}` itself, on-device, the first time a command is used, and caches the result — a database update or a newly-added device model no longer means re-touching or re-flashing every dashboard.json built against it, and it's no longer blocked by Logitech's own Harmony device catalog being unmaintained (most recent hardware was never going to be in there in the first place). Hand-pasting a single Pronto code for a one-off button is still supported side by side, unchanged — new devices choose either mode per device, not globally.
  - `AppConfig.kt`: new `IrDeviceSource` sealed class (`Inline` — the old embedded-commands shape, `SdCardRef` — a `category`/`brand`/`model` pointer). `IrDeviceConfig` carries a `source` instead of always embedding `commands` directly; `DashboardLoader.kt`'s parser and encoder round-trip both shapes, so every existing `dashboard.json` with inline-resolved commands keeps working exactly as before.
  - New `IrDatabaseRuntime.kt`: a Kotlin port of the web builder's `prontoToPattern()` (verified value-for-value against the original on real captured codes), plus the `/sdcard/astrion/ir-database/<category>.json` loader/resolver, with a two-level cache (parsed file per category, resolved step per command) so repeat button presses skip both the disk read and the Pronto math.
  - `Dashboard.kt` (tile taps) and `MainActivity.kt` (hardware hotkeys) both now resolve through this same runtime instead of each reading `IrDeviceConfig.commands` directly — one shared `IrDatabaseRuntime.resolve()`/`resolveFrom()`, not parallel copies of the same lookup.
  - Web builder (`docs/` and `src/main/assets/docs/`, identical patch to both): the "Add IR device" form now offers the two modes above instead of the old category→brand→model→command cascade fetched from the now-removed bundled catalog. `giIrCommand` (scene_grid tiles) and the Activity wizard's power/input/volume command fields are now free-text inputs with `<datalist>` suggestions instead of `<select>`s, since a referenced device's real command list only exists on the phone at runtime, not in the builder session.
- **New "IR devices" panel on the local config page (`/`), next to Icons.** A link out to the picker, plus a same-shape-as-Icons upload form for its own curated `<category>.json` files straight from the device — for when you'd rather not hand-copy them onto `/sdcard/astrion/ir-database/` yourself. New `POST /ir-database` route in `ConfigServer.kt`: validates the upload actually has `"category"`+`"brands"` before writing anything (a half-written or garbled file here would otherwise silently break every IR button sourced from it, not fail loudly once), then calls `IrDatabaseRuntime.invalidate()` — picked up on the very next command send, no restart needed. The same route also answers the picker's own "Send to my remote" button directly over the LAN (`Accept: application/json` switches the response from the page's usual redirect-with-flash-message to a plain JSON body); whether that button actually reaches the device depends on the calling browser's mixed-content policy, since the picker is HTTPS (GitHub Pages) and this server is plain HTTP — when it's blocked, downloading and copying the files by hand (or this new on-device upload form) works regardless. 5 new strings across `values/`/`values-fr/` for the panel's heading, description, link, button, and note.
- **The web builder can now read straight off this remote's own `/sdcard/astrion/ir-database/` when opened via `/builder/`.** New `GET /ir-database` (which category files are actually present) and `GET /ir-database/<category>.json` (raw contents) — same-origin, so none of the picker's mixed-content caveats apply here. The "Reference the ir-database" device form picks this up automatically: a category → brand → model quick-pick appears above the manual fields whenever this remote actually has matching files already, filling in category/brand/model *and* every real command id from that file — no more typing "known command ids" by hand for anything already copied over. The manual fields stay right below it, unchanged, for anything not on this remote yet.
- **Fixed:** ir-database category filenames are matched case-insensitively (`TV.json` now resolves the same as `tv.json`). Android's filesystem is case-sensitive, and a strict match here failed silently — not a crash, just every IR button in that category quietly doing nothing, which is a much harder thing to notice than a loud error. Files uploaded through the new `/ir-database` endpoint are also lowercased on the way in, to stop new mismatches from accumulating; `IrDatabaseRuntime`'s lookup stays case-insensitive regardless, for anything that arrives some other way (a manual copy, an older picker download, etc.).
- **Instant push sync to Home Assistant (current page + active Activity per room).** Previously the companion HA integration had to poll the local config server's `/current-page`/`/activities/active` on a timer to find out either had changed. New optional `ha_webhook_id` setting (web configurator's connection screen, `ConfigServer.kt`/`RemoteSettings.kt`) — when set, `HaClient.pushWebhook()` POSTs `{"type":"page",...}`/`{"type":"activity","rooms":{...}}` to `<ha_url>/api/webhook/<id>` the moment either changes, so the paired `sensor.active_activity_<room>`/`select.activity_<room>` entities update immediately instead of on the next poll interval. Fire-and-forget: a failed push (unreachable HA, blank id, network hiccup) is silently swallowed and never surfaces an error — polling keeps working regardless, this is purely a speedup. Wired into `Dashboard.kt`: a single collector on `ActivityRuntime.activeByRoom` covers every source of an Activity change (manual tap, Harmony, etc.) without instrumenting each call site separately, and the existing `LaunchedEffect(pagerState.currentPage)` gained the page push alongside its existing `onPageChanged()` call. Requires Astrion HA custom component 2026.8.4+, which registers the matching webhook (see its own changelog) — leave the field blank to keep behavior identical to 1.0.6.
- **Localized the remaining hardcoded UI strings** ([#51](../../pull/51) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)). Triggered by the settings pulldown's update row being hardcoded French ("Bêta disponible : dev-latest") even on English-locale devices; the audit that followed turned up a much longer list. 28 new strings added across `values/`/`values-fr/`, covering: `SettingsMenu`'s update row (now a private `UpdateRow` composable, beta + stable text), local-config line, and HA connection-status detail (was a raw enum name); `Dashboard`'s connection banner (Connecting/Auth failed/Error/Disconnected) and its unknown-card-type message; card-level strings in `MediaBrowser`, the Plex card + its detail and library dialogs (loading, "This season", episode label, Play button, empty states), the media player detail dialog's Browse button, and select/source-select/vacuum fallback text. The web builder (`docs/`, plus the device's bundled assets mirror kept in sync) had shipped its release-badge feature French-only (télécharger l'APK, Bêta indisponible, Installer sur cet appareil) and its 11 hardware-key tooltips as a French/English mix — both now use a small en/fr string table keyed on `navigator.language`, English default; fixed a "Page precedente" typo along the way. ktlint/detekt/lint/unit tests green; verified on-device (HA100, locale en-US).
- **Media player detail dialog: a Browse button.** `MediaBrowser` — a generic `media_player/browse_media` folder navigator (drill into expandable folders, tap a playable item to play it) — existed in the codebase but was never actually wired up to anything. `MediaPlayerDetailDialog` now shows a "Browse" pill under the title/subtitle when the entity reports `SUPPORT_BROWSE_MEDIA` (`Feature.BROWSE_MEDIA`, new constant — `131072`), opening it as a stacked dialog. Unlike the Plex card's own browsing (which talks straight to a Plex server), this goes through Home Assistant, so it works for whatever `media_player` integration implements `browse_media` — Sonos, Chromecast, Kodi, radio favorites, etc.
- **Plex card: browse a whole library on long-press.** Plex has no deep-link for "open this whole library" (`plex://` only addresses single items), so `PlexLibraryDialog` (new) reimplements that browse view directly in Astrion: a paginated poster grid over `/library/sections/{key}/all`, loading more as you scroll near the bottom. Long-pressing a poster now opens this for that item's own library — falling back to opening Plex directly (`media_player.select_source`) if the server didn't report a section for it. `PlexApi.PlexItem` now carries `sectionKey`/`sectionTitle` (from Plex's `librarySectionID`/`librarySectionTitle` fields) so a long-press knows which library to open.
- **Plex card: detail view with a season episode browser.** Posters use the same two-tier gesture as the rest of the app's long-press-for-detail cards (LightCard, CoverCard, MediaPlayerCard), but with tap and long-press swapped from that convention on purpose: these rows scroll under a finger, and an accidental tap mid-scroll is common, so tap is bound to the harmless action instead. A tap opens `PlexDetailDialog` — backdrop art, synopsis, genres, a "2024 · 1h 42m · ★7.8" meta line, and, for a TV episode, a horizontal browser of the rest of that season (tapping another episode re-fetches its own detail without leaving the dialog); the dialog's own Play button is what actually attempts a direct deep-link play via `play_entity` (falls back to opening the app if that's not configured or can't be reached). A long-press instead does the quick, side-effecting action directly: open Plex on the playback entity (`media_player.select_source`). Shared HTTP client, poster/thumb cache, and JSON parsing were pulled out of `PlexCard.kt` into a new internal `PlexApi.kt` object so `PlexCard.kt` and the new `PlexDetailDialog.kt` don't duplicate them.
- **Plex card: full editor support, plus Movies/TV split.** `plex` now has its own form in the web editor (`docs/index.html` + `cards.js`) — Plex server URL, token, playback entity, optional direct-playback entity, per-row toggles, and items-per-row — instead of being reachable only through the raw-JSON "Other / custom type…" fallback. `PlexCard.kt`'s single mixed "Recently Added" row is now two separate rows, `Recently Added Movies` and `Recently Added TV`, detected automatically from the Plex server's own library section types (`/library/sections`, filtered on `type == "movie"` / `"show"`) — no library name to type in, and results from multiple sections of the same type (e.g. "Movies" + "Movies 4K") are merged and re-sorted by `addedAt`. New optional options: `show_on_deck`, `show_recently_added_movies`, `show_recently_added_shows` (all default `true`), `items_per_row` (default `12`, was hardcoded), and `play_content_type` (default `"video"`; set to `"url"` when `play_entity` is HA's native Apple TV integration entity, which deep-links instead of streaming — see home-assistant.io/integrations/apple_tv/#launching-apps). The editor preview (`preview.js`) renders placeholder poster rows using new example titles in `mocks.js`'s `PLEX_MOCK`, since the plex card talks straight to a Plex server rather than an HA entity the static preview could read live state from.
- **"HA remote command" hotkey action type** ([#40](../../pull/40) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)). A dedicated hotkey action for `remote.send_command`, instead of typing it into the generic service form: pick a `remote.*` entity via autocomplete and a command from a dropdown — uses the entity's own `commands_list` when Home Assistant provides one (e.g. Apple TV), otherwise falls back to a built-in Android TV keymap with legacy ADB aliases appended. Hotkeys already saved as `remote.send_command` open in this dedicated form when edited, and listings show `→ remote <entity> / <command>`. Editor-only change (docs source + the device's bundled copy) — saved hotkey JSON has the same shape as a generic service call, so the app dispatches it unchanged, no Kotlin changes.
- **"Find my remote" ring endpoints.** New `POST /ring` on the local config server (`ConfigServer.kt`) plays the device's own ringtone/alarm/notification default sound on loop, at a chosen volume, so a misplaced tablet can be located by ear — plays on the alarm stream (`AudioAttributes.USAGE_ALARM`) so it stands a decent chance of being heard even in silent/DND, and fully restores the original alarm volume once done. Optional form fields: `volume` (1-100%, default 80), `sound` (`ringtone` | `alarm` | `notification`, default `ringtone`), `duration` (seconds, 1-60, default 15). New `POST /ring/stop` cancels an in-progress ring immediately. A ring already in progress is replaced, not layered, by a new call; the config server's `stop()` also cleans up any in-progress ring so a settings-save mid-ring can't leave it looping forever.
- **Tap feedback sound ([#33](../../pull/33) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)).** Plays the system touch sound (`Effect_Tick.ogg`, the same one native Android UI menus use) on any tappable element — scenes, buttons, page dots, dialogs, close affordances — and on hardware button presses. New `TapFeedback.kt` (`LocalTapFeedback` + `Modifier.tapClickable`, a drop-in replacement for `Modifier.clickable` that fires the sound before `onClick`), wired through `Dashboard.kt`/`MainActivity.kt`/`CardContext`. Toggleable via a new "Tap feedback" switch in the Settings panel (`SettingsMenu.kt`), on by default.
- **Bêta locale (`dev-latest`).** New `push-beta.ps1` script publishes the current debug build straight from a local Android Studio build to a rolling GitHub pre-release (`dev-latest`), alongside pushing `dev` — no CI involved, avoids the debug-keystore instability a fresh CI runner would introduce between builds. `app/build.gradle.kts`'s `debug` build type now applies `applicationIdSuffix = ".debug"` and `versionNameSuffix = "-beta"`, so the beta installs as a separate app (`com.custom.astrion.debug`) alongside the official release — no signature conflict, no need to uninstall either one to test the other. New toggle switch in the web editor (`docs/index.html`, both on GitHub Pages and the device's own `/builder/`) shows both the current official release and, when switched on, the latest beta version, each with a direct APK download link fetched live from GitHub.
- **In-app update indicator.** `SettingsMenu` now checks GitHub Releases each time the Settings panel is opened and shows a tappable "Update available" row when a newer official build exists — downloads and launches the system installer directly, no browser needed. Always compares against `/releases/latest` (never pre-releases), so this only ever notifies about official releases, on both official and beta installs.

### Changed

- **`/builder/` is now purely the dashboard/card editor.** Home Assistant connection, Harmony Hub(s), and IR Devices management moved entirely to `/` (see above) — the builder only *references* what's configured there. No more standalone/offline mode either: this only ever runs served from the device itself; the GitHub Pages copy and the paste/download-only fallback path are gone (`device.js`, `harmony.js`, `hotkeys.js`, `cards.js` comments updated to match — no more "when opened standalone" branches).
- **Activity wizard: removed the "Page to open when this Activity starts" field.** It never actually drove page navigation — the only thing it did was gate whether volume-key hotkeys got written, and it silently failed to do even that if the typed page name didn't already exist at save time. The page association (and volume-hotkey binding) now happens automatically wherever a composed Activity is actually placed on a scene_grid tile — the one place it's unambiguous, since an Activity is only ever reached via a card or hotkey. `activities.js`'s `applyVolumeHotkeysToPage()` is now `writeVolumeHotkeysForActivity(activity, pageName)`, called from `cards.js`'s `addGridItem()` using the tile's own page override if set, otherwise the page the card is on.
- **Activity wizard command pickers (Power on/off, Input, Volume up/down/mute)** are real `<select>`s when the full command list is actually known (a Harmony device, live from the hub; an inline IR device, commands embedded right there) — previously always a free-text input with `<datalist>` suggestions, regardless of whether the suggestions were complete. Still free-text-with-suggestions for an ir-database *reference* IR device specifically, since its real command list only exists on the phone's sdcard, never fully known to the builder.
- **`MainActivity.composeContent()`: the charging-dock screen is now an overlay, not a replacement.** `Dashboard()` stays composed at all times; `ChargingScreen` renders on top of it in a `Box` instead of the previous `return@setContent` that unmounted `Dashboard` entirely while docked. `screenOn = screenOn && !isDocked` still pauses heavy cards (camera/vacuum) while docked, same as before.
- **Reduced scroll lag and memory pressure on low-resource hardware** ([#39](../../pull/39) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)). Four rendering fixes targeting devices like the HA100 (1GB RAM, quad-core Cortex-A7, 4096×4096 max GPU texture): `Dashboard.kt`'s page content now uses `LazyColumn` instead of eagerly composing every card at once, so off-screen cards are disposed — stopping their camera streams, animations, and polling; `PictureElementsCard`'s floorplan PNG and `CameraCard`'s MJPEG frames are now decoded downsampled to their actual rendered size instead of full source resolution (same fix class as [#38](../../pull/38)); the vacuum "rocking" animation is now gated on screen-on state like `CameraCard` already was, and switched to `graphicsLayer` instead of `Modifier.rotate` to avoid recomposition.

### Fixed

- **Home Assistant losing control of page navigation and Activities while the remote is docked (regression in 1.0.9).** `/set-page` and `start_activity` appeared to work for ~5 seconds (the underlying state did change) then silently reverted, because `Dashboard()` — and with it the `LaunchedEffect(navTarget)` and the Activity start/stop callbacks that actually act on those state changes — was completely removed from composition while `chargeDockMonitor.state.isDocked` was true. Fixed by the overlay change above.
- **An Activity's own page association silently broke for any Activity created since the wizard's page field was removed above.** `Dashboard.kt`'s "Active Activities" overlay navigates to `activity.page` when you tap a running activity there (`tapClickable(enabled = activity.page != null)`) — a real runtime consumer of that field missed during the initial removal, which only checked `ActivityRuntime.kt`'s (unrelated, tracking-only) use of the same field. `addGridItem()` now also sets `activity.page` at the same point it writes volume hotkeys, so this keeps working without reintroducing the broken manual field.
- **IR command suggestions were always empty for an ir-database *reference* device in the Activity wizard.** `irDeviceCommandEntries()` still read a global (`irDeviceCommandHints`) that only ever existed in the old in-builder `ir.js`, removed when IR device management moved to the Devices page — the "known command ids" hints typed in while creating the device were silently never surfaced. Hints are now stored directly on the device object (`commandHints`, persisted in `dashboard.json`) and read from there by both the Devices page and the builder.
- **Card editor (and 4 other) modals in `/builder/` closing unexpectedly while picking an option from a `<select>` inside them.** The backdrop-click-to-close handler (`onclick="if (event.target === this) close()"`) could be triggered by the synthetic click some browsers/WebViews dispatch when a native `<select>` dropdown dismisses, even though the person's tap never touched the backdrop. Now requires the *press* (`mousedown`) to have also started on the backdrop, not just the click's reported target — a real backdrop tap always does, a select-dismiss artifact never does.
- **`closePreviewModal()` didn't exist** — referenced by a "✕ Close" button since before this release, on a modal nothing in the codebase currently opens. Not reachable in practice, fixed anyway rather than left as a dead reference now that the backdrop-click rewrite above also wires up its close handler.
- **Motion-wake false triggers and mid-use screen-off on the HA100** ([#49](../../pull/49) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)). The accelerometer emits glitch samples on resume around `ACTION_SCREEN_OFF`, which the old single-sample detector (raw magnitude delta > 0.9) read as motion, waking the screen randomly; separately, the fixed ~15s screen timeout ignored ongoing accelerometer activity, so the screen could go dark mid-shake while still in hand. Detector rewritten to be gravity-referenced: each sample splits into tilt (angle vs. a gravity baseline, hard-reset after any >1s sample gap, EMA-smoothed 0.9/0.1 while settled) and linear acceleration (deviation from that baseline) — wakes on `tilt > 30°` OR `lin > 2.5 m/s²` held for 3 consecutive samples. A 1.5s warm-up after `ACTION_SCREEN_OFF` drops all samples outright, and samples with `|a| < 5 m/s²` are rejected (gravity alone guarantees ≥ 9.8), suppressing the resume glitch at its source. New keep-awhile-in-hand hold: while motion stays past half the trigger threshold, `FLAG_KEEP_SCREEN_ON` is held (re-armed per sample, released after 10s of calm), fixing the mid-use screen-off. Wakelock window 4s → 5s with early release at 4.5s (2s wake cooldown unchanged). `MainActivity` now launches `singleTask`. New `MotionWake`/`ScreenTimeout` logcat tags cover setup, warm-up/subfloor drops, the consecutive-sample counter and trigger, wakelock acquire/skip/release, screen on/off transitions, and a rolling ambient-noise summary.
- **Home Assistant intermittently couldn't reach the device while its screen was off** (`Cannot connect to host ...:8080 ... Connect call failed`, both the coordinator's own `/pages` poll and `astrion.set_page`/`start_activity` calls). `ConfigServer`'s listener thread was never actually stopped by the screen turning off (only `CameraCard`'s streaming does that, since [#32](../../pull/32)) — the real cause was Android's default Wi-Fi power management suspending the radio itself while the screen was off, with nothing in the app telling it not to. New **"Keep Wi-Fi awake"** switch in Settings (`MainActivity.kt`/`SettingsMenu.kt`/`CardContext`), **off by default**: acquires a `WifiManager.WifiLock` (`WIFI_MODE_FULL_HIGH_PERF`) while enabled, toggled live with no restart (mirrors the existing "Wake on motion"/"Local config server" switches). Deliberately not on by default — this is a battery-powered handheld remote that spends most of its time idle/screen-off, and the lock disables the radio's own power-save for as long as it's held, a real continuous battery cost during exactly that state. Worth turning on if you use `astrion.set_page`/`start_activity` or the push-webhook feature from Home Assistant while the screen would otherwise be off; leave it off otherwise. No new permissions needed (`ACCESS_WIFI_STATE`/`CHANGE_WIFI_STATE`/`WAKE_LOCK` were already declared for `WifiBootReceiver`/motion-wake).
- **Update notification never fired on a beta/debug install.** `SettingsMenu`'s "Mise à jour disponible" row, and the local config page's `/check-update` badge, always compared against the official `/releases/latest` — by design, but with no fallback, so a device running the `-beta` build (`versionNameSuffix`, see 1.0.3) could never be notified about a newer `dev-latest` push, only about a newer *official* release, which for an active beta tester may never come. Both now detect a beta build via `BuildConfig.VERSION_NAME.contains("-beta")` and check `dev-latest` instead in that case. Also: a failed check (network error, GitHub rate limit, misconfigured `REPO` constant) was silently swallowed in `SettingsMenu` with no feedback anywhere — indistinguishable from "genuinely up to date" — now logged via `Log.w("SettingsMenu", ...)` so it's visible in `adb logcat`.
- **Light/cover sliders snapped back to a stale value right after release** ([#35](../../pull/35) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)). `LightCard` (brightness, color temp) and `CoverCard` (position, tilt) reset their drag override immediately on release, showing the old value until Home Assistant's `state_changed` event arrived; light brightness also showed a stale percentage while off. New shared `rememberDragFraction`/`TrackDragFraction` helper in `CardGestures.kt`, used by all three sliders, releases the override once the live value catches up — or immediately when the entity goes unavailable (e.g. a light turned off out-of-band, where catch-up can't otherwise fire at 0).
- **Oversized PNG icons caused severe scroll lag, and could silently fail to display at all** ([#38](../../pull/38) by [@ClemsonJeeper](https://github.com/ClemsonJeeper), fixes [#36](../../issues/36)). Icons were decoded via `BitmapFactory.decodeFile()` at full source resolution even though rendered at ~28dp — a 2170×550 source consumed several MB of bitmap memory plus a matching GPU texture, and sources larger than the 4096px GPU texture limit were silently dropped entirely by `OpenGLRenderer`. New `decodeIconSampled()` in `ui/IconLoader.kt` two-pass decodes (bounds-only, then sampled) targeting each card's actual rendered size — wired into `SceneGridCard`, `ButtonGridCard`, and `TitleCard`.
- **Camera cards kept streaming all night with the screen off, draining the battery ([#32](../../pull/32) by [@ClemsonJeeper](https://github.com/ClemsonJeeper)).** Since Astrion Custom is a HOME launcher, its Activity is never stopped just because the display times out — `CameraCard`'s `LaunchedEffect` (both live-stream and snapshot-polling modes) had no concept of screen state, so Compose kept recomposing and pulling frames indefinitely regardless. Root-caused via `dumpsys batterystats`: a 100%→22% overnight drain with the screen on for only 52s total, yet ~7h51m of CPU time and 2.59M WLAN TX wakelock acquisitions over the same 9.5h window. `MainActivity` now tracks screen-on state via an `ACTION_SCREEN_ON`/`ACTION_SCREEN_OFF` receiver, threaded down through `Dashboard(screenOn = ...)` → `CardContext.screenOn` (defaults to `true`, so anything not passing it explicitly is unaffected) → `CameraCard`'s `LaunchedEffect`, which now short-circuits to a no-op while the screen is off and leaves the last decoded frame in place so the card resumes showing something immediately on wake rather than flashing back to a placeholder.
- **`media_player` progress bar frozen except right after pausing ([#12](../../issues/12) reported by [@dskudrin](https://github.com/dskudrin)).** `currentMediaPosition()` used `Instant.parse()` on `media_position_updated_at`, which only accepts a literal `Z` for UTC — Home Assistant reports a numeric offset instead (`...+00:00`), so parsing always threw and was silently swallowed, falling back to the raw static position forever. Now parsed with `OffsetDateTime.parse()`, which accepts both forms. Fixes the progress bar in both the full-variant card and the long-press detail dialog, which share this function.
- **Progress bar not rebasing after a seek.** The per-second ticker's `LaunchedEffect` only restarted on entity/state/track changes, not when the server pushed a fresh position (e.g. a seek within the same track) — it kept ticking from its old captured baseline instead of the new one. Now also restarts on `media_position`/`media_position_updated_at` changes, in both the card and the detail dialog.
- **D-pad couldn't reach the Light tile at all.** `LightCard`'s tile used a raw `pointerInput`/`detectTapGestures` for tap-to-toggle and long-press-to-detail, which never enters Compose's focus system — a physical remote's D-pad had nothing to land on. Replaced with `combinedClickable`, plus a new shared `rememberLongPressKeyModifier` that restores long-press-to-detail for a *held* hardware key, which `combinedClickable`'s `onLongClick` only ever fires from a touch gesture on its own.
- **D-pad long-press-to-detail didn't work on Cover and MediaPlayer tiles either.** Same root cause and fix as the Light tile above, applied to `CoverCard`'s tile and `MediaPlayerCard`'s compact tile.
- **Hotkey `openOverlay` / `openCurrentActivityRoom` silently ignored.** `DashboardLoader.parseHotkey()` never read these two fields from `dashboard.json`, so any hotkey configured with `openOverlay: "settings"`, `openOverlay: "activities"`, or `openCurrentActivityRoom: "<room>"` loaded with both fields `null` — the physical button did nothing, neither opening the overlay nor jumping to the active Activity's page, even though the web editor generated valid JSON for both. `encodeHotkeys()` had the same gap on the write side. Both fields are now parsed and re-encoded like every other `HotkeyConfig` field.
- **"Hotkeys active on this page" badge showed nothing for `openOverlay`/`openCurrentActivityRoom` hotkeys.** `preview.js`'s `renderPreview()` used its own local `describe()`, never updated for these two actions, instead of `hotkeys.js`'s `describeHotkey()`. The JSON was already correct — only the badge's text was blank.
- **`UpdateChecker.isNewer()` false-positive on beta builds.** The `-beta` suffix added by `versionNameSuffix` broke the numeric version comparison (`"1.0.3-beta"` → the `3` was silently dropped, comparing as `1.0` instead of `1.0.3`), causing the update checker to report "update available" almost constantly on beta installs even when already up to date or ahead. Now strips any `-suffix` before comparing.

## [1.0.0] - 2026-08-22

### Added

- **scene_grid: `icon_fill` mode.** New optional `icon_fill: true` on a scene_grid card's options scales each tile's icon to fill the tile's full height (`ContentScale.FillHeight`) instead of the default fitted size (`ContentScale.Fit`) — useful for icon-only grids (`"show_labels": false`). New checkbox in the web editor's scene_grid card form, with a live preview. ([#22](../../pull/22) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))
- **Live HA entity data in the dashboard editor preview.** Card previews in the web editor (`/builder/`) now render with real state, names, and attributes fetched from Home Assistant via a new `/ha-states` endpoint, instead of always showing the static example data — falls back to the mocks automatically when there's no HA connection or the editor is opened outside the device (e.g. GitHub Pages). Entity ID fields also get autocomplete against the live entity list. ([#13](../../pull/13) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))
- **Getting Started guide.** New `GETTING_STARTED.md` — first-time setup walkthrough for a Sanytron Astrion HA100, from enabling ADB through connecting Home Assistant and loading the first `dashboard.json`, plus troubleshooting and update instructions. Linked from the README. ([#18](../../pull/18) by [@dskudrin](https://github.com/dskudrin))
- **VacuumCard: configurable room-clean service.** Room buttons used to always fire the Roborock/Xiaomi-style `vacuum.send_command` / `app_segment_clean` — silent no-op on integrations that expect their own service instead, e.g. Dreame's `dreame_vacuum.vacuum_clean_segment`. New optional `options.room_clean_action: { domain, service, parameter }` lets a card call any `domain.service` with the room/segment id written into a named field instead — `entity_id` still goes through the same `target` mechanism every other card's service call already uses. Leaving it unset keeps today's exact default call, so every existing dashboard.json is unaffected. New optional fields in the web editor's vacuum card form.
- **Hardware button → open Settings or Active Activities.** A hotkey can now be assigned `openOverlay: "settings"` or `openOverlay: "activities"` instead of a page/service/Harmony action — same two overlays already reachable by swiping down from the top bar or up from the page dots, now also bindable to a physical remote button. New "Open overlay" action in the web editor's hotkey dialog.
- **Hardware button → return to the current Activity, per room.** A hotkey can now be assigned `openCurrentActivityRoom: "<room>"` to jump straight to the page of whichever tracked Activity is currently active in that room — the direct, no-picker counterpart to `openOverlay: "activities"`, for a remote/page already dedicated to one room. Resolved live via `ActivityRuntime.activeActivity(room)`; does nothing if that room has nothing active. New "Return to current Activity" action in the web editor's hotkey dialog.
- **Configurable "return to parent" button.** Child pages (`PageConfig.parent`) no longer hardcode the hardware BACK key for jumping back to their parent — a new optional `parentKey` field picks any physical button instead (defaults to `"BACK"`, so existing configs are unaffected). If the page also binds its own hotkey on that button, the hotkey wins and the parent-jump is skipped, same precedence as before. New picker in the web editor's page dialog, shown once a parent page is selected.
- **Camera card.** New `camera` card type for displaying a Home Assistant `camera.*` feed, in two modes: live stream (MJPEG, with automatic fallback to still snapshots if the stream stalls) or snapshot-only (still frame refreshed at a configurable interval). Aspect ratio and fit (cover/contain) are configurable. Tapping the card opens a fullscreen viewer with pinch-to-zoom (1x–8x) and pan, without interrupting the live stream. ([#8](../../pull/8) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))
- **Configurable theme colors.** The app's color palette (12 semantic tokens: backgrounds, text, icons, accents, state colors) is now configurable via `dashboard.json`, with a new "Theme & Colors" section in the web editor to adjust it visually (swatch + hex field + per-token reset) without touching the JSON. An empty theme block reproduces the original palette. Existing color fields (`SwitchCard.on_color`, per-scene color in `SceneGridCard`) now use the same color picker. ([#11](../../pull/11) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))
- **Drag-to-reorder page tabs** in the web editor, alongside the existing card reordering. Editor-only, no native app changes. ([#10](../../pull/10) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))
- **Toast notifications.** The "Save to device" confirmation in the editor now uses a non-blocking, auto-dismissing toast instead of a modal `alert()`, matching the editor's other inline feedback (icon upload, Harmony discovery, etc.). ([#9](../../pull/9) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))
- **Fan card "step" style.** New `"style": "step"` option for fan cards, for fans that report discrete speed levels rather than a continuous percentage: a single row showing the current speed name/percentage with `-`/`+` buttons that call Home Assistant's `fan.increase_speed`/`fan.decrease_speed` services directly, instead of computing and sending a percentage via `fan.set_percentage` like the `simple`/`full` styles do. No power button or name row in this layout — just the stepper. Builder support in the fan card's style dropdown and a matching live preview. Existing `simple`/`full` styles and their own percentage-step config (`options.step`) are unaffected. ([#14](../../pull/14) by [@ClemsonJeeper](https://github.com/ClemsonJeeper))

### Internal

- Migrated all hardcoded `Color(0x...)` literals in Compose UI to the new theme system (`ThemeColors` / `LocalTheme`), across every card and screen (dashboard, settings, status bar, overlays).
- `HaClient`: new dedicated HTTP client with no timeout for long-lived MJPEG streaming, plus a raw-bytes fetch method.
- `ConfigServer`: new `GET /camera-snapshot` endpoint proxying a single HA camera frame to the web editor (the browser has no HA token of its own).
- `ConfigServer`: new `GET /battery` endpoint exposing the wall tablet's own battery level and charging state as JSON (`{ level, charging }`), for the companion Home Assistant integration.
- `HaClient.onResult()`: no longer assumes every WebSocket `result` payload is an array — service calls (e.g. the fan step buttons' `increase_speed`/`decrease_speed`) can return `result: null`/`{}`, which is now handled safely instead of throwing during response parsing.

### Fixed

- **Theme migration: several card tiles lost their button/icon contrast.** `CoverCard`, `LightCard`, and `MediaPlayerCard` painted their outer tile background with `theme.controlBackground` — the same token their round buttons and icon backgrounds also use — so those buttons visually disappeared into the tile (no more "circle" around the cover up/down icons, the main icon, the light card's cycle button, or the media player's off-state avatar). Tile backgrounds now use `theme.cardSurface` instead, restoring the contrast the pre-theme hardcoded colors had (matching the convention already used by `FanCard`/`ClimateCard` and by `MediaPlayerCard`'s own detail dialog).
- **Activity wizard: adding a Harmony device silently failed.** In the "Devices" step, `renderWizardHarmonyAddFields()` rendered the Hub/Device pickers straight into the same container as the "+ Add Harmony device" button — but that picker's own render call replaces its container's entire content, wiping out the button (and the placeholder selects the code then tried, and failed, to swap in) the moment a Harmony hub was reachable. Net effect: picking a hub and device had no button to add them with, so the wizard always reported "Add at least one device." on Next. Now renders the picker into its own sub-container and reuses the same shared Hub→Device wiring as the hotkey and scene-item forms. ([#16](../../issues/16) reported by [@bilbon0bud](https://github.com/bilbon0bud))
- Weather forecast bar (Clock & Weather card) rendered as a flat color instead of a two-tone gradient — restored using `theme.success` at two alpha levels.
- Compact Fan card layout used the same background color for on/off — on now uses `theme.success` at low alpha, matching the pattern already used elsewhere in the same card and in `ClimateCard`.
- Connection banner no longer distinguished "connecting" from "disconnected" (both used `controlBackground`) — connecting now uses `theme.accentSecondary`, keeping `danger` for the failure states and `controlBackground` for disconnected.

## [0.9.0] - 2026-08-19

### Added

- `ConfigServer`: new `GET /activities`, `GET /activities/active`, `POST /activities/start`, and `POST /activities/stop` routes, exposing `ActivityRuntime`'s per-room Activity state (previously only readable on-device, via the Active Activities overlay from 0.8.0) to an external controller — built for the companion Home Assistant integration's new `sensor.active_activity_<room>` and `select.activity_<room>` entities, but usable by anything that can speak plain HTTP. `/activities` lists every trackable Activity (composed or `track: true`) as `{id, name, room, icon}`; `/activities/active` reports which one (if any) is active per room; `/activities/start` starts one by id; `/activities/stop` stops whichever is active in a given room.
- `ActivityRuntime.clear(room)` — the counterpart to `markActive()`, explicitly marking a room as having nothing active. Only meant to be called once the room's Activity has actually been stopped (its own poweroff devices sent, or a Harmony hub's own `"-1"`/PowerOff confirmation arriving through `bind()`), never speculatively ahead of the stop action itself.
- `Dashboard.kt`: `stopActivity(room)` — the missing counterpart to `switchActivity()`/`startActivity()`. For a composed Activity, sends each device's own `powerOffCommand` (same as `switchActivity()`'s outgoing-diff branch, just with nothing incoming) and clears the room. For a Harmony-backed Activity, sends PowerOff to *that Activity's own hub only* — Harmony has no per-Activity stop command, a hub always runs exactly one Activity at a time, so this is the narrowest possible "stop" and, unlike the existing PowerOff hotkey, never touches a different room sharing the same hub. This closes a real gap from 0.8.0: the Active Activities overlay could show what was running per room but had no way to end just one of them, short of a blanket "turn everything off" hotkey.
- Active Activities overlay: each row now has a dedicated "Stop" button (`R.string.stop_activity` — "Arrêter" in French) wired to the new `stopActivity()`, so a running Activity can be ended directly from the list without hunting for its poweroff button elsewhere.
- `Dashboard()` gained `onActivityRuntimeReady`/`onStartActivityReady`/`onStopActivityReady` callback parameters, invoked once per `ActivityRuntime` instance (i.e. once per `dashboard.json` load, same lifetime `remember(config) { ActivityRuntime(config) }` already had). `MainActivity` stores what they hand up and feeds it to `ConfigServer` as plain lambdas — the same hoisting pattern `onSetPage` already used for the page pager, needed here because `ActivityRuntime`/`switchActivity`/`startActivity` are created inside Compose (closing over `activitiesById`, `harmonyRegistry`, `client`) rather than in `MainActivity` itself.
- Hierarchical pages: `PageConfig` gained an optional `parent` (another page's name), making it a child page in a navigation tree instead of a flat top-level one. Entering a child is unchanged — any card's own `navigateToPage`, e.g. a `scene_grid` tile with `"page": "Apple TV"` on its parent's page, exactly like flat navigation always worked. Leaving is new: the hardware BACK key, previously a no-op unless a page-specific hotkey bound it (`MainActivity.dispatchKeyEvent` only falls through to `onBackPressed()` once every hotkey handler for that key comes back empty), now jumps to the current page's parent when nothing else claims it first — so a page with its own BACK hotkey (an Apple TV's Back/Menu, say) is completely unaffected. A dashboard with no `parent` set anywhere behaves identically to before this existed.
- `PageIndicator` reworked around siblings instead of the whole flat page list: the dots now represent only the current page's siblings (every page sharing the same `parent`, root pages included), windowed to at most 5 with a small "…" on a clipped edge — the fix for large dashboards' dot row growing to take up a quarter of the screen height. A child page also shows a `‹ ParentName` chevron on the left (44dp min tap target), the on-screen twin of the hardware BACK key's new behavior. Swiping itself is unaffected by any of this — still one continuous pager over the full flat `pages` list in file order, same as always.
- Builder (`docs/js/pages.js`, `index.html`): the page dialog gained a "Parent page" dropdown, offering every page except the one being edited and any of its own descendants (`pagesUnavailableAsParentOf()`) — hierarchies can't be built into a cycle from the UI. Renaming a page that others point to as their parent cascades the new name to those children automatically; deleting a page with children asks for confirmation and promotes them to top-level rather than deleting or orphaning them to a name that no longer exists. `docs/js/preview.js`'s tab bar prefixes a child page's label with `↳` so the hierarchy is visible in the tab strip itself, not just inside each page's own settings.
- `ConfigServer`: new `GET /version` route reporting `{version, versionCode}` — built for the companion Home Assistant integration's new `update.app_update` entity, which compares it against the latest GitHub release.
- New `SelectCard` (`"type": "select"`) for any `input_select.*` or `select.*` entity — an adaptation of Home Assistant's Mushroom select card, the same way `CoverCard` adapts Mushroom's cover card: icon in a colored shape, a name/state column, and a dedicated control area (the option picker itself) below/beside/stacked per `layout` (`default`/`horizontal`/`vertical`, same 3 variants as `CoverCard`), instead of the whole tile being one flat clickable dropdown. Reads the entity's `options` attribute and current `state`, calls `<domain>.select_option` on pick — the domain (`input_select` or `select`) is read straight from `entity_id`, so one card type covers both without extra config. Answers a GitHub feature request for exactly this (e.g. picking between "TV"/"Projector" for a room's video output) — deliberately a separate renderer from `SourceSelectCard` rather than a shared/branching one, since that card is specific to media_player's `source_list`/`source` attributes and `media_player.select_source` service, none of which exist on an input_select/select entity.
- Builder (`docs/js/cards.js`, `index.html`, `docs/js/preview.js`, `docs/js/mocks.js`): added the "Select / dropdown (select)" card type to the picker, its own form (entity, name, icon color, layout), and a full live preview (new `SELECT_MOCK`, an `input_select.video_output_living_room` with "TV"/"Projector" options — the same example from the feature request) matching `SelectCard.kt`'s actual rendering, reusing the same `.pc-icon`/`.pc-namestate`/`.pc-slider`-shaped CSS the cover/light previews already established rather than introducing new classes.

### Fixed

- `MainActivity.DEBUG_KEYS` was left `true`, showing an "Unmapped key: N" Toast on screen every time an unbound remote button was pressed. Now `false`; unmapped key codes are still logged via `Log.i(KEY_TAG, ...)` for `adb logcat` when actually debugging a new remote.

## [0.8.0] - 2026-08-16

### Added

- AV Activities: any `scene_grid` tile or hotkey can now be marked `"track": true` with a `"room"`, turning it into a tracked Activity ("Watch Apple TV", "Listen to Music"...) — no separate config section, the tile's existing `entity_id`/`activityId`+`hub`/`harmonyDevice`+`harmonyCommand` is still what actually runs; `track`/`room` just add bookkeeping. At most one Activity is active per room at a time; different rooms are fully independent.
- Composed Activities (`ActivityConfig`/`ActivityDeviceConfig`, `AppConfig.activities`): for setups where more than one device needs orchestrating — the case a Harmony hub normally handles internally — Astrion can now do it itself, mixing IR/Harmony/HA devices freely within the same Activity. On switching Activities within a room, a device used by both the outgoing and incoming Activity is left alone (no needless power cycle, just a possible input change); a device only in the outgoing one gets powered off (`switchActivity()` in `Dashboard.kt`). Referenced from a `scene_grid` tile's new `"activity"` field; always implicitly tracked.
- IR Devices (`IrDeviceConfig`, `AppConfig.irDevices` — replaces the old `IrActivityConfig`/`irActivities` macro list): a registry of named commands per physical device (`"power"`, `"hdmi1"`, `"volume_up"`...), resolved once at build time from either a community `ir-database/` entry or a hand-pasted Pronto Hex code. This is the fully-offline baseline for AV control — no Harmony hub, no Home Assistant, no cloud dependency, works even if every external service involved disappears.
- `ActivityRuntime`: scans every page/hotkey for `track: true` items, and every entry in `AppConfig.activities`, once per `dashboard.json` load; tracks which Activity is active per room (`activeByRoom`), updated via `trackTap()` (right after `SceneGridCard` fires a single-action tile), `markActiveById()` (after a composed Activity finishes its start sequence), and `bind()` (mirrors a Harmony hub's *live* Activity state into the matching room, so an Activity started from elsewhere — another remote, an HA automation, the physical Harmony remote — shows up too).
- `HarmonyHubClient`: `currentActivityId`/`activityState` (`StateFlow`), populated from the hub's unsolicited `connect.stateDigest?notify` push frames (`activityId` + `activityStatus`, distinguishing "starting" from "running" — confirmed against several real hub captures) and from a new one-shot `getCurrentActivity()` request/response call made right after connecting, since the push only fires on the *next* change.
- `HarmonyHubRegistry.clientsByLocalId` — exposes every configured hub's client, not just the default one (`client()`), for callers like `ActivityRuntime.bind()` that need to wire up all of them.
- New "Active Activities" overlay: swipe **up** from the bottom edge (page-indicator dots) — the mirror gesture of the existing swipe-down-for-Settings — lists every currently active Activity grouped by room; tapping one jumps straight to its page. Reached only by this gesture, like Settings, so it never appears in the page pager or its dots.
- Builder: new "IR Devices" section — the community `ir-database/` cascade picker (category → brand → model → command) now feeds a per-device command registry instead of a one-shot macro; a "Import all commands from this model" button pulls every command from a picked model in one go (ids already present are left alone, so re-importing after a manual rename doesn't clobber it); a manual Pronto Hex paste path covers devices/buttons not in the database; each saved command can be renamed inline (✎, no browser `prompt()`, consistent with the rest of the builder).
- Builder: new "Activities" section — a page-by-page wizard for composed Activities, mirroring Logitech Harmony's own Activity setup flow rather than one long form: pick a type (just prefills a name) → name/room/icon/page → select devices (local IR, Harmony hub+device, or a bare HA entity id — mixable) → one screen per selected device for its power-on/power-off/input commands, on/off-cycle toggles, and a delay → which device handles volume → review (flags any device with no commands set — it would silently do nothing at runtime) → save. Editing an existing Activity jumps straight to the name/room screen. A device already added can be edited in place, not just removed and re-added.
- Volume routing for a composed Activity: after picking a volume device, a further screen picks its volume-up/volume-down/mute commands (skipped for an HA device — it always uses the fixed `media_player.volume_up`/`volume_down`/`volume_mute` services). On save, if the Activity has a `page`, these are written as page-scoped hotkeys on it (`PageConfig.hotkeys`, which already override global bindings while that page is on screen — no new runtime mechanism needed). If that page already has a hotkey on `VOLUME_UP`/`VOLUME_DOWN`/`MUTE`, the builder confirms before overwriting rather than silently taking priority over an existing binding.
- `HotkeyConfig` gained `irDevice`/`irCommand` (send a local IR command directly from a physical-button binding, same as a `scene_grid` tile already could) and `devices` (a bookkeeping hint — see next point).
- `TrackedActivity`/`HotkeyConfig`/scene_grid items gained an optional `devices` list: which physical devices an Activity is known to involve, independent of whether it's composed or a lightweight `track`-only tile. `switchActivity()` in `Dashboard.kt` now reads this from *whatever* Activity was previously active in a room — Harmony-backed or composed — instead of only from a composed `ActivityConfig`'s own device list. This closes a real gap: switching from a Harmony Activity to a composed one had no way to know a device shared between them (e.g. a soundbar with only a `PowerToggle` command, no discrete on/off) was already on, and could send an unwanted toggle that turned it off instead of confirming it stayed on.
- Builder (`docs/js/cards.js`): a `scene_grid` tile with "Track as Activity" checked can now list which physical devices it involves (comma-separated ids) — mainly useful on a Harmony-backed tile, since Astrion has no other way to know which devices a Harmony Activity touches. `scene_grid` tiles also gained an IR device+command picker and a "Composed Activity" picker referencing the new Activities section.
- `MainActivity.kt`: `runHotkey()` now dispatches `irDevice`+`irCommand` bindings directly through the device's own IR blaster (`ConsumerIrManager`) — the same local IR path `scene_grid`/composed Activities already had, now available on physical-button bindings too. Closes the loop for a composed Activity's volume routing when the volume device is local IR.
- New `title` card type — a section header for grouping cards on a page (title + optional subtitle, independently alignable), ported from Home Assistant's Mushroom Title card. Where Mushroom uses its own `title_tap_action`/`subtitle_tap_action` (a full HA `ActionConfig` — call-service, navigate, more-info, url, assist...), this instead reuses the same `title_`/`subtitle_`-prefixed action fields `scene_grid` already has, so it also covers Harmony/IR/composed-Activity actions with nothing new to learn. Also supports an optional `icon` (a PNG file path, same `BitmapFactory.decodeFile` convention every other icon in the app uses — deliberately not a bundled/named vector set) and a `divider` line filling the rest of the row, Bubble-Card-style; either forces the title row left-aligned regardless of `alignment`, since that layout has no sensible centered/right-aligned form. An optional `color` (same ARGB hex convention as `scene_grid`'s) tints the title text and, when `divider` is on, the line at reduced alpha to match — the subtitle and icon are untouched by it, icons are never tinted anywhere in the app. Builder: new "Title" entry under "Common" with a title/subtitle/alignment/icon/divider/color form; a tappable title needs the "Other / custom type…" raw-JSON editor instead, since re-saving through the simple form — like every card type here — rewrites the card's options from scratch.

### Changed

- Builder: editing an existing Harmony-backed tile (`scene_grid` or `apple_tv_remote`) that has no explicit `hub` no longer silently defaults the Hub dropdown to the first configured hub — with more than one hub, that default could be the wrong one and would get silently re-saved as correct. The dropdown now shows empty and the existing "pick a hub" validation blocks saving until one is chosen explicitly.
- Builder: ids generated from a name (IR devices, Activities) now strip accents via Unicode NFD normalization instead of mangling them — `"Xbox Série X"` becomes `"xbox_serie_x"`, not the previous `"xbox_s_rie_x"`.
- `SettingsOverlay`/`ActivitiesOverlay`: the swipe-to-close gesture now lives on a dedicated strip (bottom for Settings, top for Activities — mirrored) instead of on the same node as `verticalScroll`, which meant the scroll consumer ate every vertical drag before the close gesture ever fired. Incorporates the `SettingsOverlay` fix from [PR #3](https://github.com/dckiller51/astrion-custom-dashboard/pull/3) (see Fixed, below), applied to `ActivitiesOverlay` as well.

### Fixed

- Builder (`docs/js/cards.js`): `fillGiHarmonySection()` was defined twice (dead duplicate); removed.
- Builder: `dashboard.json`'s `irDevices`/`activities` were silently dropped every time the builder reloaded a config — both `pages.js`'s "paste JSON" import and `device.js`'s auto-reload-from-device path had a field whitelist that pre-dated those two sections.
- Builder: the Activities section's IR-device picker didn't refresh after saving or removing an IR device (only the `scene_grid` form's picker did, via a different refresh path).
- `Dashboard.kt`: `SettingsOverlay` called `SettingsMenu(ctx, onClose = onClose)`, but `SettingsMenu` only ever declared `ctx` — a pre-existing compile error.
- `HaModels.kt`: `EntityState.attrString()`/`attrDouble()`/`attrBoolean()` used the throwing `.jsonPrimitive` extension on a raw attribute value, crashing the app on any non-primitive attribute instead of returning null — switched to a safe `as? JsonPrimitive` cast. Reported against v0.7.0 in [issue #2](https://github.com/dckiller51/astrion-custom-dashboard/issues/2) with a Kodi `media_player` entity, whose `media_content_id` attribute is a nested object (`{"imdb": "...", "tmdb": "..."}`) rather than the plain string most other media players report: the `media_player` card's progress bar crashed on `variant: "full"` once playback started, and crashed the long-press Detail dialog outright, both of which key their `remember`/`LaunchedEffect` on that attribute. `MediaPlayerCard.kt`/`MediaPlayerDetailDialog.kt` also switched that key from `attrString("media_content_id")` to `attr("media_content_id")?.toString()`, so the progress bar still resets correctly between tracks for Kodi (a plain `attrString()` would now just always read null for it, post-fix, since it's never a primitive) as well as for every player that reports it as a plain string.
- Top status bar clock now honors the device's 12/24h setting instead of always rendering 24h, and is centered to the full bar width rather than drifting with the Wi-Fi/battery glyph widths. `ConfigServer` no longer crashes with `EADDRINUSE` on connection save, and retries once on a delay if the initial `:8080` bind fails. An unmapped physical BACK press no longer destroys the launcher activity; saving new connection settings now reconnects HA/Harmony clients and restarts `ConfigServer` in place instead of relying on `Activity.recreate()`, which was unreliable on the HA100's Android 8.1. All three via [PR #3](https://github.com/dckiller51/astrion-custom-dashboard/pull/3), contributed by [@ClemsonJeeper](https://github.com/dckiller51/astrion-custom-dashboard/commits?author=ClemsonJeeper).

## [0.7.0] - 2026-08-14

### Added

- `MediaPlayerCard`: full Mushroom-style rewrite of the `compact` layout — round album-art/icon avatar, name + state row, and a control row below that switches between transport buttons (previous/play-pause/next, plus optional power/shuffle/repeat) and volume controls (mute, -/+ buttons, or a slider) via a small swap button, matching Home Assistant's own Mushroom media-player card behavior. Tap the tile to toggle play/pause; long-press opens the new `MediaPlayerDetailDialog` (see below).
- `MediaPlayerCard`: new `MediaPlayerDetailDialog`, opened by long-pressing the compact tile (same gesture `LightCard`/`CoverCard` use) — large artwork, a live progress bar, the full transport row, a volume slider with mute, and a power toggle when the entity supports it.
- `MediaPlayerCard`: `variant` config option (`"compact"` (default) / `"full"`) replaces the old fixed layout; the `full` variant keeps the dedicated-media-page look (large art, `top_buttons`) but adds a live progress bar (computed from `media_position` + `media_position_updated_at`, advancing every second while playing) and the same feature-gated transport/volume rows as the compact tile.
- `MediaPlayerCard`: `use_media_info` (default true — shows `media_title`/`media_artist`/`app_name` instead of the entity's friendly name/state), `show_volume_level` (appends "⸱ N%" to the state line), `media_controls` (comma list: `on_off,shuffle,previous,play_pause,next,repeat`), and `volume_controls` (comma list: `mute,buttons,set`) config options — every button is checked live against the entity's `supported_features` bitmask, so a control requested in config but not supported by the entity never renders.
- `MdiIcons`: `Play`, `Pause`, `SkipPrevious`, `SkipNext`, `VolumeHigh`, `VolumeOff`, `Repeat`, `Shuffle`, `Cast`, `CastOff` glyphs, replacing the generic Material icons the old media player card used. `Cast`/`CastOff` also become the compact tile's (and full page's) fallback avatar — device on/off — whenever the entity reports no `entity_picture`, instead of guessing a play/pause icon.
- New `media_state_playing`/`paused`/`idle`/`buffering`/`on`/`off` string resources (English and French), used on the state line whenever the entity reports no `media_title`/`media_artist`/`app_name`.
- Dashboard builder (`docs/index.html`): `media_player` cards now have their own dedicated form (previously only the raw-JSON fallback) — variant, name, entity, `use_media_info`/`show_volume_level` checkboxes, per-control checkboxes for `media_controls`/`volume_controls`, and a `top_buttons` JSON field (full variant only) — plus a real preview matching the on-device look, including the swap-button behavior and shuffle/repeat's tinted-not-swapped active state.
- `CoverCard`/`LightCard`: new optional controls, matching Home Assistant's Mushroom cards. Covers can show open/stop/close buttons, a position slider, and a tilt slider (any combination); lights can show a brightness slider, a colour temperature slider, and colour swatches. If more than one is turned on for a card, a small button lets you flip between them. All configurable from the dashboard builder, with a matching preview. Existing dashboards aren't affected unless you turn these on.
- Dashboard builder: the remote preview now lights up the physical buttons that already have a hotkey assigned to them (on the current page or globally), so it's easy to see at a glance what's already mapped.

### Changed

- `MediaPlayerCard`: the compact layout's control row now offers mute and full transport (previous/play/pause/next, optionally shuffle/repeat/power) — the previous compact layout only ever showed a fixed volume up/down pair with no way to control playback.
- Shuffle and repeat only have one glyph each (no separate "active" variant) — `MediaPlayerCard`/`MediaPlayerDetailDialog` tint the button instead of swapping its icon when shuffle is on or repeat isn't `"off"`.
- Dashboard builder: the remote preview is now a proper redrawing of the physical remote instead of a photo, and much closer to true scale — icon/text sizes, how many cards fit on screen before scrolling, and button positions all now match the real device.

### Fixed

- `CoverCard`/`LightCard`: the new sliders' fill bar was centering and shrinking from both sides instead of filling left to right.
- Dashboard builder: the zoomed-in preview rendered everything about 9% smaller than the real device.
- Dashboard builder: some elements in the remote preview (screen, buttons) could end up slightly out of position depending on your browser.
- Dashboard builder: two of the physical media buttons could render slightly past the edge of the remote.

### Removed

- `remote.png` — the static photo the dashboard builder used to overlay the preview onto, now unused: the remote preview is a fully HTML/CSS recreation of the physical device (see *Changed* above).

## [0.6.0] - 2026-08-11

### Added

- Settings: new "Local config server" switch, next to "Wake on motion". `ConfigServer` (the unauthenticated `:8080` admin surface — connection settings, `dashboard.json`, icon uploads, `/builder/`) now defaults to on but can be switched off once a device is fully set up, closing that LAN-reachable surface for good; it's started/stopped live from `MainActivity` with no app restart, and can always be switched back on from this same on-device screen (never from the web page itself, to avoid a lockout). Persisted in `SharedPreferences` alongside the existing motion-wake setting.
- `CoverCard`: `layout` config option (`"default"`, `"horizontal"`, `"vertical"`), matching Home Assistant's Mushroom cover card:
  - `"default"` (new) — icon + name/state row, with the open/stop/close buttons full-width on their own row below.
  - `"horizontal"` — icon + name/state on the left, buttons inline on the right (the card's only layout before this release, kept as-is).
  - `"vertical"` (new) — icon, name, state, and buttons all centered and stacked in one column.
- `CoverCard`: dedicated icons via new `MdiIcons` entries — `WindowShutterClosed`/`WindowShutterOpen` for the main icon (reflects the cover's last known open/closed state, updated live), and `CoverUp`/`CoverDown` for the raise/lower buttons, replacing the earlier generic Material icons (`Blinds`, `KeyboardArrowUp`/`KeyboardArrowDown`).
- `CoverCard`: the raise button now disables (dimmed, non-clickable) once the cover is fully open, and the lower button disables once fully closed — the stop button stays active either way.
- `LightCard`: `layout` config option (`"default"`, `"horizontal"`, `"vertical"`), matching Home Assistant's Mushroom light card — same pattern `CoverCard` got in 0.6.0:
  - `"default"` (new) — icon + name/state row, with a slim brightness bar full-width below it (only drawn while on and dimmable).
  - `"horizontal"` (new) — icon + name/state, single row, no bar.
  - `"vertical"` (new) — icon, name, and state, all centered and stacked in one column.
- `LightCard`: `use_light_color` option — when true and the light reports an `rgb_color`, the icon (and the default layout's brightness bar) tint with that colour instead of the plain amber "on" look.
- `LightCard`: `show_brightness` option (default true) — shows the live brightness as `"N%"` on the state line while on; set false to always show a plain "On" instead.
- `LightCard`: icon now swaps between a filled and an outline lightbulb depending on on/off state (Mushroom's own look), instead of a static tile background swap — new `MdiIcons.LightbulbOn`/`MdiIcons.LightbulbOff` (the latter a slashed bulb, clearer than a plain outline for "off"), same pattern as `WindowShutterOpen`/`Closed`.
- `LightCard`: tap toggles, long-press opens the same brightness/colour detail dialog the bubble-style card used (`LightDetailDialog`, unchanged) — see Removed, below.
- New `light_state_on` / `light_state_off` / `light_brightness_pct` string resources (English and French — "Allumé"/"Éteint" rather than the generic switch-style "Marche"/"Arrêt") so the state line is translated: 0% brightness (or a plain off state) reads as "Off"/"Éteint", anything else reads as its live percentage.
- Dashboard builder (`docs/index.html`): `light` cards now have their own dedicated form (previously grouped in with the generic name/entity/icon-only cards) with a `Layout` picker and the two new checkboxes, plus a real preview for all three layouts driven by a generic example entity (color_temp/xy profile, no real device name or local IP baked into a publicly-shipped file), including the icon colour tint and brightness bar.
- `HaLabels.coverState()` / `assets/ha_labels/*.json`'s new `cover_state` category (open/closed/opening/closing/stopped/unknown, English and French) — used as `CoverCard`'s fallback label for covers that report no `current_position` attribute at all, matching the pattern already used for `VacuumCard`.
- Dashboard builder (`docs/index.html`): `cover` cards now have their own dedicated form (previously grouped in with the generic name/entity/icon-only cards) with a `Layout` picker, plus a real preview for all three layouts driven by an example entity (`cover.volet_chambre_noham`).
- Dashboard builder preview: `scene_grid`/`button_grid` tiles now render their actual configured icon (uploaded to `/sdcard/astrion/icons/` on the remote) as an image, instead of only showing the tile's name — via a new `GET /icons/<filename>` endpoint on `ConfigServer.kt` that serves files back out of that folder (the same one `POST /icons` writes into). Only works when the builder is opened from the remote itself (`/builder/`); the standalone GitHub Pages copy has no device behind it to serve the files from and falls back to the same blank-icon layout the app uses when a file is missing.
- Dashboard builder: every icon field (name/entity cards, `cover`, and `scene_grid`/`button_grid` items) now has a "Choose…" button and a live thumbnail alongside the path text field, opening a picker that lists every icon already uploaded to the device as clickable thumbnails (new `GET /icons-list` endpoint on `ConfigServer.kt`) — typing the `/sdcard/astrion/icons/xxx.png` path by hand is still possible but no longer the only way. Same "only works when served by the device itself" caveat as the preview thumbnails above; the picker explains that inline instead of doing nothing when it can't reach `/icons-list`.
- `CoverCard`: new `CoverDetailDialog`, opened by long-pressing the card's icon/name area (same gesture `LightCard` uses) — a vertical position pill you can drag or tap to set an exact 0–100% position, plus quick-preset chips (Closed/25%/50%/75%/Open) for jumping straight to a common position. Uses `cover.set_cover_position` (or `open_cover`/`close_cover` at the 0%/100% ends).
- `LightCard`: the `"default"` layout's brightness bar is now a real slider instead of a read-only indicator — drag or tap it to raise/lower brightness directly from the card, no need to open `LightDetailDialog` first. Made a bit thicker (20dp) to stay easy to grab on a touch panel.

### Removed

- `BubbleLightCard` (`bubble_light` card type) — its slider-pill interaction and long-press detail popup are superseded by `LightCard`'s new layouts (the `"default"` layout's brightness bar is itself now draggable, see Added above) plus its existing long-press into `LightDetailDialog`. Existing `bubble_light` cards in a saved `dashboard.json` should be changed to `"type": "light"` (same `entity_id`/`name` options still apply).
- Dashboard builder: the separate "Light — dimmable (bubble_light)" entry — the builder's `light` entry now covers both use cases.

### Fixed

- `CoverCard`'s status label wasn't translated at all — it was a hardcoded `"$position% open"` English string. It now reads "Open"/"Closed" (translated) at 100%/0% position, and a translated "N% open" in between — new `cover_open`, `cover_closed`, `cover_position_open` string resources (English and French).
- Dashboard builder preview: `scene_grid`/`button_grid` tiles used a generic fixed-size placeholder (`preview-tile`) that didn't match the real on-device sizing at all. Tile height now matches the real dp values (74dp/58dp icon vs. text-only for `scene_grid`, 68dp/48dp per button for `button_grid`), and `scene_grid` tiles use each scene's configured color (with the same luminance-based text-color logic as `SceneGridCard.kt`) instead of a flat gray background.
- Dashboard builder preview: `scene_grid`/`button_grid` real-icon preview (see Added, above) never actually worked — `docs/js/cards.js`'s `iconUrl()` correctly pointed at `/icons/<filename>`, but `ConfigServer.kt` only ever wired up `POST /icons` (upload); the matching `GET /icons/<filename>` to serve an uploaded icon back out didn't exist, so every icon request 404'd and silently fell back to the blank-icon look. Added the missing route.
- `Dashboard`: jumping to a page (menu-card `navigateToPage`, a physical hotkey, or a page-indicator dot) used `pagerState.animateScrollToPage()`, which visibly scrolls through every page in between the current one and the target — on the HA100's weak CPU this showed up as the previous page flashing up right before the intended one landed. Switched to `scrollToPage()` (instant, no animation) for all three, so a direct jump lands directly; swipe gestures between adjacent pages are unaffected.

## [0.5.0] - 2026-08-10

### Added

- Multi-hub Harmony support: `RemoteSettings` now stores a list of Harmony hubs (name/IP/ID) instead of a single hardwired one, each with a stable app-generated `localId` used to reference it from `dashboard.json`. The settings page (`/`) lists them as repeatable rows with a "+ Add a hub" button; existing single-hub installs are migrated automatically on first read.
- `HarmonyHubRegistry`: owns one `HarmonyHubClient` per configured hub, connects/disconnects them together, and resolves which client a `hub` reference should use — falling back to the first configured hub when a `hub` field is absent, so single-hub `dashboard.json` files keep working unchanged.
- "Auto-detect ID" per hub row, plus automatic detection on the IP field losing focus: resolves a hub's numeric ID from its IP alone via `HarmonyHubDiscovery` (`setup.account?getProvisionInfo`, `Origin: http://sl.dhg.myharmony.com`), so hubs no longer need their ID copied by hand from Home Assistant's `harmony_<id>.conf`.
- `HarmonyHubClient.getConfig()`: fetches a hub's full config over the existing WebSocket connection — every paired device with its IR commands, plus every Activity — the same data Home Assistant keeps in `harmony_<id>.conf`. Exposed via `GET /harmony-config?hub=<localId>` (and `GET /harmony-hubs` to list configured hubs), and cached to `astrion/harmony_<hubId>.json` next to `dashboard.json` on every successful fetch — served from that cache automatically if the hub is temporarily unreachable.
- The dashboard builder (`docs/`) is now bundled as app assets and served locally at `GET /builder/`, alongside the existing GitHub Pages copy — no separate computer or internet access needed to build a `dashboard.json`.
- Builder: `dashboard.json` now loads automatically when the builder is opened from this device (`js/device.js`), with a "Save to device" button that writes it directly and live-reloads the dashboard — replacing the copy-paste/download-only flow. Falls back to the original flow when the builder is opened outside the app (GitHub Pages).
- Builder: hotkeys and scene tiles can now target a Harmony hub through a cascading **Hub → Device → Command** or **Hub → Activity** picker (`js/harmony.js`), fed live from the paired hub(s), instead of typing raw device/command/activity IDs. Falls back to the original plain text fields when the builder isn't served by the app.
- `SceneGridCard` scenes support `harmonyDevice`/`harmonyCommand` (alongside the existing `activityId`), and both now respect an optional `hub` field. `AppleTvRemoteCard` also gained an optional `hub` option. `CardContext.startHarmonyActivity`/`sendHarmonyCommand` and `HotkeyConfig` both take a `hub` parameter/field for this, defaulting to the first configured hub when omitted.

### Removed

- `CustomIrCard` (`custom_ir` card type): superseded by IR Activities (0.4.0) — a `scene_grid` tile with an `irActivity` action covers the same local, hub-free IR sending, with a proper Category → Brand → Model → Command picker in the dashboard builder instead of hand-written `freq`/`pattern` arrays. Existing `dashboard.json` files with `custom_ir` cards need to migrate their buttons to an IR Activity + `scene_grid`.
- `docs/index.html`: `custom_ir` option removed from the "Card type" dropdown.
- `docs/README.md`: `custom_ir` removed from the list of raw-JSON-fallback card types.

### Changed

- `cards/impl/TileCards.kt` split into one file per card: `CoverCard.kt`, `FanCard.kt`, and `SwitchCard.kt` (renamed from `TileCards.kt`, now containing only `SwitchCard`). Same package, same class names — no config or registration changes needed. Each card previously shared a couple of small private UI helpers (e.g. `CircleBtn`); those are now duplicated per file instead, so each card's look can evolve independently.
- Local config page (`/`, `ConfigServer.kt`): the Home Assistant connection panel and the dashboard.json panel are now shown side by side (`panels-grid`, 2 columns above 700px, single column on narrower screens) instead of stacked.
- Local config page: the "Updates" section moved from a separate block at the bottom of the page into a compact badge next to the Home Assistant/hubs status badges — shows the current version, or an amber "update available" badge that installs directly when tapped.

## [0.4.0] - 2026-08-06

### Added

- `FanCard`: new `full` style (auto-picked when the entity reports `preset_modes` and/or an `oscillating` attribute), mirroring `ClimateCard`'s layout from 0.3.0 — a dedicated power button, preset-mode chips (or a percentage stepper for percentage-only fans), and an oscillate on/off toggle.
- `FanCard`: `preset_modes` config override, mirroring `ClimateCard`'s `fan_modes`/`swing_modes` overrides — reorders or restricts the preset chips shown, in the order given, instead of always reading the entity's raw attribute order. `"off"` is always excluded from the chips since the card's own power button covers it.
- `FanCard`: oscillate toggle chip, reusing `HaLabels.swingMode()`'s existing on/off translation for its label (new `fan_oscillate_caption` / `fan_preset_caption` string resources, English and French).
- `FanCard`: `style` config option (`"auto"` (default), `"simple"`, `"full"`), same pattern as `ClimateCard`.
- `SceneGridCard`: scene tiles now render an optional PNG `icon`, loaded the same way as `ButtonGridCard`'s `icon` field — previously accepted in `dashboard.json` but silently ignored. Tiles switch to an icon-above-label layout when one is set.
- `docs/index.html`: `scene_grid` form gained `color` and Harmony `activityId` fields, previously only settable by hand-editing `dashboard.json`.
- `docs/index.html`: the preview pane now renders dashboard content inside an image of the physical remote (`remote.png`), sized to the real HA100 screen resolution (480×800), with a mocked status bar (Wi-Fi / time / battery) so the space it takes up on-device is accounted for. Tapping the screen expands it into a larger modal for easier editing.
- `SceneGridCard`: `show_labels` config option — when `false`, tiles show the icon only, no name text underneath.
- `ClimateCard` / `FanCard`: `show_captions` config option — when `false`, hides the "Mode"/"Fan"/"Swing" (climate) or "Preset"/"Oscillate" (fan) labels above their chip rows.
- `docs/index.html`: forms for `climate`, `fan`, and `scene_grid` gained checkboxes for the new `show_captions`/`show_labels` options above, including in the preview mock for climate/fan.
- **IR Activities**: named sequences of raw IR sends, executed locally through the device's own IR blaster (`ConsumerIrManager`) — no Harmony hub or Home Assistant required, for setups without one.
  - `AppConfig`: new `irActivities` list (`IrActivityConfig`/`IrStepConfig` — id/name/steps, each step a `freq`/`pattern`/optional `delayAfterMs`), parsed from `dashboard.json`'s new `"irActivities"` array.
  - `CardContext`: exposes `irActivities` (by id) to cards.
  - `SceneGridCard`: new `irActivity` scene action (alongside `entity_id`/`activityId`/`page`) — runs the named activity's steps in order on the card's own coroutine scope, waiting `delayAfterMs` between steps.
  - `docs/ir-database/`: community-contributed device codes (`index.json` category manifest + one JSON file per category, e.g. `tv.json`), keyed by category → brand → model → command, storing each command as Pronto Hex (universal raw IR timing, protocol-agnostic — works for NEC, JVC, Sony, etc. via one decoder instead of one per protocol).
  - `docs/index.html`: new "IR Activities" section — cascading Category → Brand → Model → Command pickers (loaded from `ir-database/`), per-step delay, and a Pronto → `freq`/`pattern` decoder that resolves each step at build time, so `dashboard.json` ships fully-resolved steps and the app never needs to parse a protocol or ship the database itself. `scene_grid` items gained an "IR activity" picker alongside their existing action fields.

### Changed

- `ClimateCard` / `FanCard`: power buttons now share a consistent color scheme — red when off, green when on — instead of `ClimateCard`'s off-only red accent and `FanCard`'s on-only green accent, so both cards read the same way at a glance.
- `docs/index.html`: `button_grid`/`scene_grid` items in the editor list can now be clicked to edit them in place, instead of only add/remove.
- `docs/index.html`: the small in-frame preview no longer shows the card type label, edit/remove icons, or `hint` text (entity id, "example data", etc.) — only the expanded modal does — since none of that renders on the real device.
- `docs/index.html`: split the single ~1400-line file into `styles.css` + `js/{mocks,pages,cards,hotkeys,ir,export,preview}.js`, matching the section comments that were already there. Same behavior, just easier to find and edit a given piece — and `docs/js/` now needs to be deployed alongside `index.html`, not just the one file.

### Fixed

- Wi-Fi sometimes failed to reactivate after a restart, requiring a manual trip into Android's system settings. Added a `WifiBootReceiver` (`BOOT_COMPLETED`) that re-enables Wi-Fi automatically at startup if it isn't already on, with the `ACCESS_WIFI_STATE`/`CHANGE_WIFI_STATE` permissions it needs to read and change the radio state.
- `docs/index.html`: `button_grid`/`scene_grid` cards with more columns than fit the small in-frame preview width caused a horizontal scrollbar; grid columns now shrink to fit instead.
- `SceneGridCard`: tile height (icon vs. text-only) was decided per-tile, so a grid mixing scenes with and without an `icon` rendered uneven row heights. It's now decided once for the whole grid — every tile matches as soon as any one of them has an icon.

## [0.3.0] - 2026-08-04

### Added

- Dashboard builder (`index.html`): `clock_weather` cards now have a dedicated form (weather entity, 12h/24h time format, number of forecast rows, optional calendar entity) instead of the raw-JSON fallback used for unmodeled card types, plus a matching preview tile in the right-hand pane.
- Dashboard builder (`index.html`): `vacuum` cards now have a dedicated form (name, entity, map image entity, map rotation/height, room list with segment IDs) instead of the raw-JSON fallback, plus a matching preview tile in the right-hand pane.
- `ClimateCard`: swing-mode chips (`climate.set_swing_mode`), matching the existing hvac/fan-mode chips — reads the entity's `swing_modes` attribute, translated via a new `HaLabels.swingMode()` / `assets/ha_labels/*.json` `swing_mode` category.
- `ClimateCard`: per-feature icon/label display, matching Home Assistant's own climate tile-card (`style: icons`). New `hvac_mode_style`, `fan_mode_style`, and `swing_mode_style` `dashboard.json` options (`"label"` or `"icons"`). Icons are hand-built from official MDI path data via a new `MdiIcons` object — no new Gradle dependency.
- `ClimateCard`: `hvac_modes` config override, mirroring the existing `fan_modes`/`swing_modes` overrides — reorders or restricts the hvac chips shown, in the order given, instead of always reading the entity's raw attribute order.
- `ClimateCard`/`MdiIcons`: dedicated glyphs for fan "quiet"/"silent", numeric fan speeds 1–5, swing on/off, and the heat_cool/auto mode, replacing the earlier generic fan/arrow placeholders with the device-accurate icons.
- `ClimateCard`: a small caption above each chip row ("Mode"/"Fan"/"Swing") so the swing chips are legible in text-label style instead of showing just "On"/"Off" with no context — new `climate_hvac_caption`, `climate_fan_caption`, `climate_swing_caption` string resources (English and French).
- Dashboard builder (`index.html`): `climate` cards' form now covers `hvac_modes`, `swing_modes`, `hvac_mode_style`, `fan_mode_style`, and `swing_mode_style`.
- Dashboard builder (`index.html`): `climate`, `clock_weather`, and `vacuum` cards now render a real example preview (icons, translated labels, forecast bar, room chips) driven by a representative mock entity, instead of placeholder text like "(translated condition)".

### Changed

- `ClimateCard`: the "off" hvac mode no longer shows as a chip — the card's header already has a dedicated power-off button, so a chip for it was a redundant control.
- `ClimateCard`: mode chip rows (hvac/fan/swing) now wrap and balance evenly across rows (e.g. 5 items → 3+2) instead of a hard 4-per-row cutoff that silently hid anything past the 4th mode.
- `ClimateCard`: tightened padding/spacing and chip/button sizes to reduce the card's overall height on small panels.

### Fixed

- `ClockWeatherCard` didn't translate its current-condition label — it just capitalized the raw Home Assistant state string (e.g. `partlycloudy` → "Partlycloudy") instead of going through `HaLabels`. It now calls `HaLabels.weatherCondition()` like the rest of the app.
- `assets/ha_labels/*.json`'s `weather_condition` category only covered 10 of the 16 conditions Home Assistant's weather platform can report; added the missing `clear`, `lightning-rainy`, `snowy-rainy`, `windy-variant`, `hail`, and `exceptional` entries (English and French).
- `VacuumCard` had the same issue as `ClockWeatherCard`: its activity-state label (docked/cleaning/paused/idle/returning/error) was capitalized from the raw state instead of going through `HaLabels`. It now calls `HaLabels.vacuumState()`. Fan-speed/cleaning-mode names are left as-is since they're integration-specific, not a fixed HA value set.
- `ClimateCard`'s fan-mode chips were built from a hardcoded `["low", "medium", "high", "auto"]` guess whenever `dashboard.json` didn't override them, ignoring the entity's own `fan_modes` attribute — unlike `hvac_modes`, which already read it correctly. Chips could show options the device doesn't actually support (and silently fail when tapped). It now prefers the entity's reported `fan_modes`, falling back to the config override or the hardcoded list only if the integration reports nothing.
- `ClimateCard`'s current-temperature label ("Now 27°") was a hardcoded English string instead of going through `res/values*/strings.xml` like the rest of the app's UI text; added `climate_current_temp` (English and French) and switched the card to `stringResource()`.
- `ClimateCard`'s hvac-mode chips silently dropped anything past the 4th mode (`modes.take(4)`) — an entity reporting `off, fan_only, heat, cool, heat_cool, dry` only ever showed the first four. The cap is gone; see "Changed" above for the row-wrapping that replaces it.

## [0.2.0] - 2026-08-03

### Added

- Local web configurator on `http://<remote-ip>:8080`: set the Home Assistant URL/token and Harmony Hub IP/ID after install, without adb or a rebuild.
- `dashboard.json` upload/download and icon upload from the same local page.
- In-app update checker: reads this repository's GitHub Releases, downloads a newer APK, and opens the system installer.
- Settings panel is now a dedicated overlay reached by swiping down from the top edge, separate from `dashboard.json`'s page list — no longer swipable as a regular page or listed in the page-indicator dots.
- Local configurator address (`http://<ip>:8080`) shown directly in the Settings panel.
- App text (Settings panel, etc.) now follows the system language via standard Android string resources (English default, French translation included).
- Home Assistant state values (climate `hvac_mode`/`fan_mode`, weather conditions, vacuum states) now translated per language via `assets/ha_labels/<lang>.json`, instead of showing the raw English values from the integration.

### Changed

- `SettingsMenuCard` moved out of `cards/impl/` into `ui/SettingsMenu.kt` — it's no longer a `dashboard.json`-driven card, it's rendered directly by the Settings overlay.
- Connection settings (Home Assistant, Harmony Hub) no longer come from `BuildConfig`/`secrets.properties` baked in at compile time — every install starts unconfigured and is set up via the local web page instead. This is what makes a single prebuilt APK safe to share publicly.

### Fixed

- Home Assistant state translations were cached at process start and never refreshed after a system language change; they now reload correctly when the language changes.
- Update-install flow now checks the "install unknown apps" permission and catches installer failures instead of letting them crash the app (and, with it, the local web server).

## [0.1.0] - Initial build

- Initial custom dashboard app, based on [@baes-cloud](https://github.com/baes-cloud/astrion-dashboard)'s original project.
- Swipeable card-based dashboard driven by `dashboard.json`.
- Home Assistant WebSocket connection, Harmony Hub integration, physical hardware key routing.
